spring:
  application:
    name: courier-back
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:postgres}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: ${APP_PORT:8080}
  servlet:
    context-path: /api

app:
  security:
    jwt:
      public-key-uri: ${JWT_PUBLIC_KEY_URI:}
      private-key-uri: ${JWT_PRIVATE_KEY_URI:}
      issuer: courier
      access-token-ttl: 15m
    session:
      refresh-token-ttl: 30d
